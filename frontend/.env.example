# Frontend Environment Variables Template

# Backend API Base URL
# In production with reverse proxy / Nginx / Docker: /api/v1
# When frontend is on separate domain (e.g. Vercel): https://api.yourdomain.com/api/v1
VITE_API_BASE_URL=http://localhost:3001/api/v1
